## moduli = è un file contenente un codice in Python (funzioni, classi, ecc...)

#import messaggi as sms          ## importiamo l'altro file messaggi per usarlo (si puo cambiare non per la chiamate della funzione)

#altrimenti si può importare direttamente le funzionie

from messaggi import *      ## importa tutte le funzioni

hello()        ## per usare le sue funzioni fare 'nome file.funzione'
bye()                   ## il nome delle funzioni se importate


help("modules")     ## per sapere queli moduli usare o ci sono